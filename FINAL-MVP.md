# ContextIQ Final MVP — Days 1–7

This archive is the cumulative source for the ContextIQ MVP. It keeps the Day 1 landing/workspace foundation, Day 2 authentication UI, Day 3 document upload/library, Day 4 real RAG backend, Day 5 citation-aware document chat/viewer, Day 6 persistent conversations/search/settings, and Day 7 streaming/security/Docker/README polish.

Runtime-generated directories are intentionally excluded from the archive: `node_modules`, `.next`, Python `.venv`, `backend/storage`, local SQLite database files, and real `.env` files.
