'use client'; import {FormEvent,useState} from 'react';import Link from 'next/link';import {AuthCard,AuthFooter,Field,GoogleButton} from '@/components/auth';import {useAuth} from '@/components/auth-provider';
export default function LoginPage(){const{login}=useAuth();const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setBusy(true);const d=new FormData(e.currentTarget);try{await login(String(d.get('identifier')),String(d.get('password')));window.location.href='/dashboard'}catch(x){setError(x instanceof Error?x.message:'Login failed.')}finally{setBusy(false)}}return <AuthCard title="Welcome back" subtitle="Sign in to your private document workspace."><form onSubmit={submit} className="space-y-4"><Field label="Email or Username" name="identifier" autoComplete="username" required/><Field label="Password" name="password" type="password" autoComplete="current-password" required/><div className="flex justify-end"><Link href="/auth/forgot-password" className="text-sm text-cyan-200 hover:underline">Forgot password?</Link></div>{error&&<p className="rounded-lg border border-red-900/60 bg-red-950/30 p-3 text-sm text-red-300">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950 disabled:opacity-50">{busy?'Signing in…':'Login'}</button><div className="flex items-center gap-3 text-xs text-slate-600"><span className="h-px flex-1 bg-slate-800"/>OR<span className="h-px flex-1 bg-slate-800"/></div><GoogleButton/></form><AuthFooter>New to ContextIQ? <Link className="text-white hover:underline" href="/auth/register">Create an account</Link></AuthFooter></AuthCard>}
