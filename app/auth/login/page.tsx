"use client";
import {FormEvent,useState} from "react"; import Link from "next/link"; import {AuthCard,AuthFooter,Field,GoogleButton} from "@/components/auth";
export default function LoginPage(){const[error,setError]=useState("");function submit(e:FormEvent){e.preventDefault();setError("Real login requires the FastAPI authentication service. No fake session was created.");}return <AuthCard title="Welcome back" subtitle="Sign in to continue to your ContextIQ workspace."><form onSubmit={submit} className="space-y-4"><Field label="Email or Username" name="identifier" autoComplete="username" placeholder="you@example.com" required/><Field label="Password" name="password" type="password" autoComplete="current-password" placeholder="Your password" required/><div className="flex items-center justify-between text-sm"><label className="flex items-center gap-2 text-slate-400"><input type="checkbox" name="remember"/>Remember me</label><Link href="/auth/forgot-password" className="text-slate-200 hover:underline">Forgot Password?</Link></div>{error&&<p className="rounded-lg border border-red-900/60 bg-red-950/30 p-3 text-sm text-red-300">{error}</p>}<button className="w-full rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Login</button><div className="flex items-center gap-3 text-xs text-slate-600"><span className="h-px flex-1 bg-slate-800"/>OR<span className="h-px flex-1 bg-slate-800"/></div><GoogleButton/></form><AuthFooter>New to ContextIQ? <Link className="text-white hover:underline" href="/auth/register">Create an account</Link></AuthFooter></AuthCard>}
