<<<<<<< HEAD
'use client';import {useState} from 'react';import Link from 'next/link';import {AuthCard,AuthFooter,Field} from '@/components/auth';
export default function Reset(){const[done,setDone]=useState(false);return <AuthCard title="Choose a new password" subtitle="Use a strong password you do not reuse elsewhere.">{done?<p className="rounded-xl border border-emerald-500/20 bg-emerald-500/5 p-4 text-sm text-emerald-200">Reset-token delivery can be connected to the same FastAPI account service in deployment.</p>:<form onSubmit={e=>{e.preventDefault();setDone(true)}} className="space-y-4"><Field label="New Password" name="password" type="password" required/><Field label="Confirm Password" name="confirm" type="password" required/><button className="w-full rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Update password</button></form>}<AuthFooter><Link href="/auth/login" className="text-white">Back to Login</Link></AuthFooter></AuthCard>}
=======
"use client";
import {FormEvent,useState} from "react"; import Link from "next/link"; import {AuthCard,AuthFooter,Field} from "@/components/auth"; import {validatePassword} from "@/lib/password-policy";
export default function ResetPasswordPage(){const[done,setDone]=useState(false),[error,setError]=useState("");function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget),p=String(d.get("password")||""),c=String(d.get("confirm")||"");const x=validatePassword(p);if(x.length)return setError(x.join(" "));if(p!==c)return setError("Passwords do not match.");setDone(true)}return <AuthCard title="Choose a new password" subtitle="Use a strong password you don’t reuse elsewhere.">{done?<div className="space-y-4"><p className="rounded-lg border border-slate-700 bg-slate-950 p-4 text-sm text-slate-300">Password update UI is ready. Connect the FastAPI reset-token endpoint to persist the change securely.</p><Link href="/auth/login" className="block w-full rounded-xl bg-white px-4 py-2.5 text-center text-sm font-semibold text-slate-950">Return to Login</Link></div>:<form onSubmit={submit} className="space-y-4"><Field label="New Password" name="password" type="password" autoComplete="new-password" required/><Field label="Confirm New Password" name="confirm" type="password" autoComplete="new-password" required/>{error&&<p className="text-sm text-red-300">{error}</p>}<button className="w-full rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Update Password</button></form>}<AuthFooter><Link className="text-white hover:underline" href="/auth/login">Back to Login</Link></AuthFooter></AuthCard>}
>>>>>>> 96caef8b5731e0359bc665a7d85c03e5a003a67a
