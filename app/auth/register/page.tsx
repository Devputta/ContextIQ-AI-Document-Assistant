"use client";
import {FormEvent,useState} from "react"; import Link from "next/link";
import {AuthCard,AuthFooter,Field,GoogleButton} from "@/components/auth"; import {validatePassword} from "@/lib/password-policy";
export default function RegisterPage(){const[error,setError]=useState("");
function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);const u=String(d.get("username")||"").trim(),m=String(d.get("email")||"").trim(),p=String(d.get("password")||""),c=String(d.get("confirmPassword")||"");if(!u||!m||!p||!c)return setError("Please complete all required fields.");if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(m))return setError("Enter a valid email address.");const x=validatePassword(p);if(x.length)return setError(x.join(" "));if(p!==c)return setError("Passwords do not match.");setError("Real registration requires the FastAPI authentication service. No fake account was created.");}
return <AuthCard title="Create your ContextIQ account" subtitle="Secure access to your documents and conversations."><form onSubmit={submit} className="space-y-4"><Field label="Username" name="username" autoComplete="username" placeholder="Choose a username" required/><Field label="Email" name="email" type="email" autoComplete="email" placeholder="you@example.com" required/><Field label="Password" name="password" type="password" autoComplete="new-password" placeholder="Minimum 8 characters" required/><Field label="Confirm Password" name="confirmPassword" type="password" autoComplete="new-password" placeholder="Re-enter your password" required/>{error&&<p className="rounded-lg border border-red-900/60 bg-red-950/30 p-3 text-sm text-red-300">{error}</p>}<button className="w-full rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Register</button><div className="flex items-center gap-3 text-xs text-slate-600"><span className="h-px flex-1 bg-slate-800"/>OR<span className="h-px flex-1 bg-slate-800"/></div><GoogleButton/></form><AuthFooter>Already have an account? <Link className="text-white hover:underline" href="/auth/login">Login</Link></AuthFooter></AuthCard>}
