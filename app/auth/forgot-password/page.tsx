"use client";
import {FormEvent,useState} from "react"; import Link from "next/link"; import {AuthCard,AuthFooter,Field} from "@/components/auth";
export default function ForgotPasswordPage(){const[sent,setSent]=useState(false);function submit(e:FormEvent){e.preventDefault();setSent(true)}return <AuthCard title="Reset your password" subtitle="Enter your email and we’ll send instructions if an account is associated with it.">{sent?<div className="space-y-4"><p className="rounded-lg border border-slate-700 bg-slate-950 p-4 text-sm text-slate-300">If an account matches that email, a password-reset link has been requested. Check your inbox.</p><Link href="/auth/login" className="block w-full rounded-xl bg-white px-4 py-2.5 text-center text-sm font-semibold text-slate-950">Back to Login</Link></div>:<form onSubmit={submit} className="space-y-4"><Field label="Email" name="email" type="email" autoComplete="email" placeholder="you@example.com" required/><button className="w-full rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Send Reset Link</button></form>}<AuthFooter><Link className="text-white hover:underline" href="/auth/login">Back to Login</Link></AuthFooter></AuthCard>}
