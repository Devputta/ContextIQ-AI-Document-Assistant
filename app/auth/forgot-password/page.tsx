'use client';import {useState} from 'react';import Link from 'next/link';import {AuthCard,AuthFooter,Field} from '@/components/auth';
export default function Forgot(){const[sent,setSent]=useState(false);return <AuthCard title="Reset your password" subtitle="Password reset email delivery is a deployment configuration step; no fake reset is claimed.">{sent?<p className="rounded-xl border border-white/10 bg-slate-950 p-4 text-sm text-slate-300">If an account matches that email, reset instructions would be sent once an email provider is configured.</p>:<form onSubmit={e=>{e.preventDefault();setSent(true)}} className="space-y-4"><Field label="Email" name="email" type="email" required/><button className="w-full rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Request reset</button></form>}<AuthFooter><Link href="/auth/login" className="text-white">Back to Login</Link></AuthFooter></AuthCard>}
